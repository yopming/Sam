var samDashboardControllers = angular.module('samDashboardControllers', []);

/*
 * Dashboard
 */
samDashboardControllers.controller('SamDashboardCtrl', ['$scope', '$http',
  function($scope, $http) {
  }
]);
